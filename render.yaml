services:
  - type: web
    name: rescurfood-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        value: foodlink_secure_jwt_secret_2026
      - key: FIREBASE_PROJECT_ID
        value: foodlink-007
